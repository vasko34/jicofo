jicofo {
  jibri {
    # Enable or disable automatic recording. Enabled by default. When
    # enabled, recording starts when a user from a predefined set joins and
    # stops when all such users leave. Set to false to disable.
    auto-record = true
  }
}
